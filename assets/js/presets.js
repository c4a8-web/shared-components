const Presets = {
  default: {
    min_width: 320,
    max_width: 1600,
    fallback_max_width: 800,
    steps: 5,
    sizes: '(min-width: 50rem) 50rem, 90vw',
    figure: 'never',
    loading: 'auto',
  },
};

export default Presets;
